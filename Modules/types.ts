export interface Person {
  name: string;
  email: string;
}

export type Color = "red" | "green" | "blue";

// creating an interface and a type in the types.ts file and exporting them. Then, we can import them in the app.ts file and use them in our code.
